package org.rsmod.content.skills.crafting.util


object CraftingConfig {
    /**
     * When true and a crafting action has exactly one possible recipe, the "how many" prompt is
     * skipped and the maximum possible amount is crafted. */
    const val SKIP_SINGLE_RECIPE_PROMPT: Boolean = false
}
